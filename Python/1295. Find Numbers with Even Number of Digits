question - https://leetcode.com/problems/find-numbers-with-even-number-of-digits/?envType=daily-question&envId=2025-05-04

solution 

class Solution(object):
    def findNumbers(self, nums):
        total = 0

        for num in nums:
            if len(str(num)) % 2 == 0:
                total += 1

        return total
        
